# テンプレートの変更履歴

`graph-doc-template` 本体の変更を記録する。**派生プロジェクト固有の変更は
各リポジトリで記録すること。** このファイルはテンプレートの履歴だけを扱う。

このファイルは共有ファイルなので `git merge template/main` で更新される。
**マージしたらここを読む。** 何が変わり、何をすべきかが分かる。

現在の版:

```bash
python -m tools.graph --version
```

## 版の付け方

| 上げる箇所 | 意味 | 取り込む側の作業 |
| --- | --- | --- |
| major | **移行作業が要る** | 下記の「移行手順」に従った作業が必要。マージだけでは壊れる |
| minor | 追加のみ | 通常のマージで済む。新しい警告が出ることはある |
| patch | 文言・不具合の修正 | 通常のマージで済む |

**破壊的な変更を入れるときは、必ず「移行手順」を書く。**
書けないなら、その変更は入れない。手順のない破壊的変更は、
放置されたリポジトリを黙って壊す。

---

## 1.14.2 — 2026-09-11

### 修正

雛形に続いて**ノードの本文を AI レビューにかけた**。11 ノードに対する指摘のうち、
共有ファイル（`docs/00-meta/` とサンプルノード）に当たるものをここで直した。

**`META-01` の 85 行が自動生成ブロックより後ろに落ちていた。** 見出しの並びはこうだった。

| 位置 | 見出し |
| --- | --- |
| 5 節 ルール一覧 | `G011` `G012` `G013` `G014` |
| 6 節 | 運用サイクル |
| 7 節 | 雛形 |
| （5 節の外） | `G016` / `G017`、`G015` — 切り離されていて、番号も逆 |
| （5 節の外） | **自動生成ブロック** |
| （その後ろ） | `G018`、`G019` — **文書の 12%** |

自動生成ブロックは「関連ドキュメント（自動生成 / 手で編集しない）」という文書の
締めである。その下に本文が続くとは読む人は思わない。`G018` を直したい人は
**「手で編集しない」と書かれた塊を越えて下まで行く**ことになる。

**`check` はこれを見ていない。** 自動ブロックはリンクの集計から外れるだけで、
並びは検査していない。**7 リポジトリすべてで同じ状態だった。**

`G015`〜`G019` を 5 節の中へ番号順に戻し、自動生成ブロックを文書の最後に置いた。
**本文は 1 文字も書き換えていない**（並べ替えのみ。行の多重集合が一致することを
機械で確かめている）。減ったのは余分な区切り線 2 本だけである。

**雛形の一覧に `contract-interaction` が無かった（2 箇所）。** `META-01` の 7 節と
`META-02` の「雛形」、どちらの表も `contract.md` と `contract-http.md` の 2 つしか
載せていなかった。CLAUDE.md は「チャットの操作（コマンド・ボタン）→
`--template contract-interaction`」と明記していて、実際に tournament-bot で 8 ノード、
discord-bot-docs で 3 ノードがこの雛形から起きている。`META-01` には
`usecase-runbook` も無かった。

**`META-02` の段落が 1 つ迷子になっていた。** 「既定を汎用にしてあるのは…HTTP に
限らないため」は契約の既定雛形の話だが、手順書の節の末尾に置かれていた。
どの既定を指すのか読めないので、雛形の表の直後へ移した。

**`META-02` の採番で、桁を切り替えるときに既存をどうするかが抜けていた。**
並びは id の文字列順（`sorted_nodes`）なので、**桁が混ざると `UC-100` が `UC-99` より
前に来て目次が崩れる。** 既存も `rename` で振り直すこと、99 まで待たない理由を足した。

### サンプルノード

サンプルは 7 リポジトリに配られ、**複製の出発点になる。** 4 件を直した。

| ノード | 何が問題だったか |
| --- | --- |
| `DOM-01` | 本文が「保持期限」を使うのに属性表に無かった。「期限を過ぎたものは**自動的に**取消になる」が `UC-01` の例外フロー `E2`（確定要求の処理中に取消する）と食い違っていた |
| `ARCH-01` | 「1 台の API サーバで賄える範囲（超えたらこの文書は無効）」が測れない。**無効になったかを誰も言えない** |
| `ARCH-02` | 「恒久的な失敗は再実行しない」と表に書いた直後に「恒久的な失敗を再実行しても害はない」と書いていた |
| `CON-01` | 雛形が両方とも持っている「変更するときの影響」が無かった。`REQUIRED_SECTIONS["contract"]` は空なので `G014` も見ていない |

`DOM-01` は「取消への遷移は**期限切れを検出した時点**で起きる」と書き直した。
`UC-01` は手を入れていない。`G015` が「依存先が変わった」と挙げるが、
**見たうえで追従不要と判断している**（`E2` はそのまま整合する）。

### 取り込む側の作業

**無い。** `docs/00-meta/` とサンプルノードは共有ファイルなので、マージで入る。

**サンプルノードを既に撤去しているリポジトリでは競合が出る。**
`DOM-01` などが modify/delete として挙がるので、**削除側を採る**。

```bash
git rm docs/20-domain/dom-01-sample-booking.md
```

README の「取り込む」に書いてある解決方法と同じである。

---

## 1.14.1 — 2026-09-11

### 修正

**雛形の末尾にあった `作成日: {{DATE}}` を 6 つの雛形から外した。**
書いた人がほぼ全員消しており、**運用として成立していなかった。**

派生 7 リポジトリの全ノードを数えた結果である（目次を除く 198 ノード）。

| 層 | 作成日あり | なし |
| --- | --- | --- |
| `10-architecture` | 0 | 18 |
| `20-domain` | 1 | 34 |
| `30-usecases` | 10 | 73 |
| `40-contracts` | 0 | 23 |
| `50-adr` | 0 | 123 |

残っている 11 件も 2026-08-23 から 09-08 の数日に固まっていて、**それ以降は 1 件も無い。**

外した理由は 3 つある。

1. **`history.py` が既に「日付を手で書かせると必ず腐る。履歴は git が正確に
   持っているのでそちらを使う」と書いている。** 雛形だけがこの方針から外れていた
2. **`sync` は自動生成ブロックの前に `---` を入れる。** 雛形の `---` と重なるので、
   作成日を残したノードでは区切り線 2 本に挟まれた 1 行になっていた
3. 毎回消す手間が発生していた

**ADR の `- 日付:` は残した。** こちらは 123 件すべてで維持されていて、
決定した日付として本文の情報になっている。

**`adr` の日付と状態に記入案内を足した。** 派生リポジトリの ADR 130 件では、
取り下げるときに `- 状態: deprecated（[[ADR-0012]] が置き換えた）` の形で
置き換え先を添える運用が定着していたが、**雛形のどこにも書かれておらず、
新しく書く人は知りようがなかった。** 本文の状態がフロントマターの `status` の
写しで、**機械では照合していない**ことも書いていなかった
（数えた時点でずれているノードは無い）。

**`contract-interaction` の中で「なし」と「無し」が混ざっていた。**
同じファイルの引数の節が「なし」、未確認の節が「無し」。
`schema.py` の `G014` の説明に合わせて「なし」に統一した。

**`usecase` の「概要」表に記入案内が無かった。** 同じ位置にある
`usecase-runbook` の表にはセル内に案内がある。`アクター`（人とは限らない）と
`価値`（機能ではなく、得られる結果）に足した。

**`contract-interaction` の案内にあるコマンドの引数順を `contract` に揃えた。**
同じコマンドが 2 通りの順で載っていた。

### 取り込む側の作業

**無い。** 既存の文書は変わらない。マージのあと `new` で起こしたノードに
`作成日:` が付かなくなるだけである。

**既にある `作成日:` を消す必要も無い。** 消すなら場所は次で分かる。

```bash
grep -rn "^作成日:" docs/
```

雛形をプロジェクト向けに書き換えている場合、この版と競合することがある。
**競合したら自分の側を残してよい。** 雛形の中身はプロジェクトごとに違ってよい。

---

## 1.14.0 — 2026-09-11

### 追加

**`G019` — Markdown の表が途中で切れていないかを見る（エラー）。**

表はヘッダ行と区切り行で始まり、空行か本文で終わる。途中に段落や空行が入ると、
**そこから先の行は表ではなくただの文字列として描画される。**

**`G018` と同じ形の問題である。** グラフの検査は全部通るのに、
GitHub 上の見た目だけが壊れる。実際に 2 件見つかった。

| どこ | 何が起きたか |
| --- | --- |
| `graph-project-template` の README | 1.8.0 でスキルの案内文を行の間に挿し込み、**以降の 14 行が本文として流れた** |
| `tournament-bot` の DOM-01 用語表 | 段落 3 つを挟んで **1 行が取り残された** |

**どちらも `check` は通っていた。** `G013` の用語表パーサは行ベースなので、
取り残された行すら拾えている（実測で 11 行すべて見えていた）。
**機械は困らず、読む人だけが困る。**

判定は 2 つだけ。`|` で始まる行のうち、**直前の行も `|` で始まる**（表の続き）か
**次の行が区切り行**（正しい表の先頭）のどちらでもないものを落とす。

- **コードブロックの中は見ない。** 規約文書は悪い例を載せるので、
  見ると自分の文書で落ちる
- 段落を挟んだ **2 つの表は正しい**（2 つ目にヘッダと区切りがある）
- `docs/` の下だけでなく **README や CONTRIBUTING も見る。**
  実際に壊れていたのは README だった

**警告ではなくエラーにした。** 表が切れていれば既に壊れて表示されており、
「承知のうえで放置してよい」場合が無い。

**誤検出を出さないことを実測で確かめてから入れた。** 7 リポジトリ 435 ファイルに
当てて、本物 2 件・誤検出 0 件。ERROR なので、外すと派生の CI が一斉に落ちる。

### 取り込む側の作業

**表が切れている文書があれば、取り込んだ時点で main の CI が落ちる。**
落ちるのは正しい。その表は**取り込む前から GitHub 上で壊れて表示されている。**

```bash
python -m tools.graph check | grep G019
```

直し方は、挟まっている段落や空行を表の外へ出すこと。行ごとに補足を書きたいなら、
セルの中に入れるか、表の後にまとめて書く。

**`README.md` は `merge=ours` なので、上流の直しは伝播しない。**
今回の 1 件目がまさにそれで、上流では直っていたのに派生側に残り続けていた。
README のずれを見るには次を使う。

```bash
git diff HEAD template/main -- README.md
```

---

## 1.13.0 — 2026-09-10

### 追加

**`/grill` に、原典（`mattpocock/skills` の `grilling`）との差分表を足した。**
このスキルは原典を作り直したものだと 1 行だけ書いてあったが、**何が同じで
何が違うのかが読めなかった。** 骨格（決定木・ラウンド・frontier）は同じで、
違うのは事実の調べ方・決定の行き先・用語集の 3 点だけである。

**原典に委譲しない理由も明記した。** テンプレートは公開リポジトリで、
`mattpocock/skills` を入れていない利用者がいる。**このファイルだけで完結させる。**

**原典にしかなかった 2 つを取り込んだ。**

- **時間のかかる調査はサブエージェントに投げる。** 全文検索、実装の走査、
  git 履歴の集計が対象
- **そして待たない。** 調査中のものは「まだ片付いていない前提」として扱い、
  **その調査に依存する問いだけ**を次のラウンドへ回す。
  **調査 1 件で全体を止めない**

**ラウンドの書式を具体化した。** 「番号を振り、推奨案を添える」とだけ書いて
あったので、出方が毎回ぶれていた。`❓ Q1` / `➡️` の形を示し、**選択式の GUI に
差し替えてよい**ことも書いた。大事なのは「まとめて出す」「各問に推奨案が付く」
の 2 つで、表示の手段ではない。

**起動する場面を `description` に足した。** 従来は「要件が曖昧なとき、
issue を書く前、ADR を起こす前」の 3 つ。ここに 2 つ足した。

- 受け入れ条件が未確定なまま実装に着手しそうなとき
- **`schema.py` の語彙や層を変える前**（全ノードに波及し、取り込む側に
  移行作業が要る。着手してからでは戻せない）

### 取り込む側の作業

なし。通常のマージで済む。

---

## 1.12.2 — 2026-09-10

### 変更

**Python の下限を 3.12 に上げた。**1.12.1 では 3.10 としていたが、これは
本番ホスト（Ubuntu 22.04）の版に合わせたものだった。**本番の側を 3.12 に
動かしたので、合わせる先が変わった。**

Ubuntu 22.04 の apt に 3.12 は無いため、deadsnakes PPA から `python3.12` を
入れ、Bot ごとの venv を作り直した。**システムの `python3` は 3.10 のまま**で、
apt や unattended-upgrades が依存しているので触っていない。
パッケージは `pip freeze` の版をそのまま入れ直したので、変わったのは Python だけ。

これで開発機・CI・本番のすべてが 3.12 になり、**幅を持たせる理由が無くなった。**
1.12.1 で足した floor ジョブ（3.10 で回す）は本体と同じ版になるので外した。

**下限は「試している中で最も古い版」であって、願望ではない。** 3.11 で動くかは
誰も試していないので、分からないと書くのが正しい。

### 取り込む側の作業

**3.12 未満で回している場所があれば止まる。** `python --version` を確かめる。
CI の `python-version` も 3.12 以上にする（テンプレートの `graph-check.yml` を
そのまま使っているなら、マージで入る）。

3.10 や 3.11 で回し続ける必要があるなら、`MIN_PYTHON` と CI と README の
3 つを揃えて下げる。`test_python_floor` がずれを見張っている。

---

## 1.12.1 — 2026-09-10

### 修正

**`linkify` が Windows で CRLF を書いていた。** 書き出し 9 箇所のうち、
`linkify` に足したものだけが `newline="\n"` を落としていた（1.11.0 の手落ち）。
Windows の `write_text` は既定で LF を CRLF に変換する。

**気付けないのが本体の問題である。** git が `.gitattributes` の
`text=auto eol=lf` でコミット時に正規化するので `git status` はきれいなまま、
作業ツリーだけが CRLF に化ける。壊れていることが表に出ない。

### 追加

**書き出しの改行と文字コードを機械で見るようにした**（`test_newline_discipline`）。
`tools/graph/*.py` を構文木で走査し、テキストとして書き出しているのに
`newline="\n"` か `encoding="utf-8"` を渡していない箇所があれば落ちる。

残り 8 箇所は手で気を付けて書かれていた。**手で気を付ける方式は、
書き出しを足すたびに同じ確率で失敗する。** 数が増えるほど当たりが出る。

- `open()` も見る。バイナリ（`"b"`）は変換が起きないので対象外
- 走査が 0 件でも検査は通ってしまうので、**「見つからない」と「問題が無い」を
  取り違えない**ための件数の下限も置いた
- `linkify` 側にはバイト列で見る振る舞いのテストも足した。テキストとして
  読み直すと Python が改行を正規化して、CRLF でも気付けない

**Python の下限を宣言どおりに検査するようにした**（`test_python_floor`）。
README には前から `Python 3.10+` と書いてあったが、**CI は 3.12 しか
回していなかった。** `version.py` の `MIN_PYTHON`、`graph-check.yml` の
floor ジョブ、README の 3 つを突き合わせ、ずれたら落とす。

**下限は本番ホストが決めている。** Lightsail は Ubuntu 22.04 で Python 3.10.12、
OS ごと入れ替えない限り動かせない。開発機の 3.12 のほうが後から付いてきた側で、
`tools/graph` が 3.11 以降の機能を使っていないことは確認済み。

`graph-check` に floor ジョブが増える。テストと `check` だけを 3.10 で回す。
図の再生成や `sync` の鮮度は版に依らないので見ない。

### 取り込む側の作業

なし。通常のマージで済む。

**ただし作業ツリーが既に CRLF になっている場合、マージしても直らない。**
git が持っているのは LF なので、コミットの中身は正しい。ローカルの実バイトを
揃えるなら 1 回だけ次を回す（コミットは 1 つも増えない）。

```bash
git add --renormalize . && git checkout -- .
```

---

## 1.12.0 — 2026-09-10

**サンプルのファイル名を、サンプルと分かる名前にした。** あわせて README の
記載を実装に合わせた。

### 何が起きていたか

サンプルの ARCH-01 は `arch-01-system-overview.md` という名前だった。
**これは「システム全体」を書くときに誰もが選ぶ名前である。**

派生プロジェクトが同じ名前を使うと、`reset-samples` が消す前に
**中身がマージされて混ざる。** 実際、ある派生プロジェクトの ARCH-01 に、
そのプロジェクトに存在しない要素（サンプルの「ワーカー」）の記述が入り込んだ。
`reset-samples` はタグで探すので、この衝突を防げない。

派生 3 件で同じ衝突が起きていた。

### 決めたこと

サンプル 5 件のスラッグに `sample-` を付けた。

| 旧 | 新 |
| --- | --- |
| `arch-01-system-overview.md` | `arch-01-sample-system-overview.md` |
| `arch-02-worker-execution-model.md` | `arch-02-sample-worker-execution-model.md` |
| `dom-01-booking.md` | `dom-01-sample-booking.md` |
| `uc-01-confirm-booking.md` | `uc-01-sample-confirm-booking.md` |
| `con-01-confirm-booking.md` | `con-01-sample-confirm-booking.md` |

**プロジェクトが `sample-` で始まる名前を選ぶことはない。**
これで衝突が構造的に起きなくなる。

### README の直し

実装に追いついていなかった箇所を直した。

- **ルール一覧が `G014` で止まっていた。** `G015`〜`G018` を追記した
- **コマンド表の途中に段落が挟まって、表が分断されていた。**
  GitHub 上では以降の行が表として描画されていなかった
- `linkify` と `render --aggregate` がコマンド表に無かった
- 構成の説明が「ルール ID G001〜G013」のままだった

### 取り込む側の作業

**通常のマージで済む。** サンプルは既に消しているはずなので、
新しい名前で入ってきたものを `reset-samples --yes` が消すだけ。

```bash
git fetch template && git merge template/main
python -m tools.graph reset-samples --yes
python -m tools.graph sync
```

**ただし、サンプルと同じファイル名を自分のノードに使っている場合は、
先に改名しておくこと。** そのままだと修正/削除の競合になる。

```bash
python -m tools.graph rename --from ARCH-01 --slug <このプロジェクトらしい名前>
```

## 1.11.0 — 2026-09-10

**本文の参照を、GitHub 上でもクリックできる形にした。**

### 何が起きていたか

本文の参照は `[[ID]]` で書く決まりだったが、**これは Markdown の標準記法ではない。**
GitHub 上ではただの文字として出て、読み手はクリックできず、id を見て
自分でファイルを探すことになる。

loader は最初から**相対リンクも同じ参照として拾っていた。**
つまり書き換えてもグラフは一切変わらない。

### 足したもの

**`linkify` コマンド。** 本文の `[[ID]]` を、その id の文書への相対リンクに直す。

```bash
python -m tools.graph linkify
```

- **書くときは `[[ID]]` のままでよい。** `sync` と同じ位置づけの整形コマンド
- **コードブロック・コードスパン・HTML コメントの中は触らない。**
  規約や雛形に載せた「書き方の例」まで変換すると、例として成立しなくなる
- **存在しない id は残す。** リンク切れは `G004` の仕事で、
  ここで黙って消すと検査をすり抜ける
- `[[ID|表示名]]` は `[表示名](パス)` になる

あわせて `make linkify` / `make linkify-check` と、CI の検証を 1 手足した。
`make all` にも入っている。

### 取り込む側の作業

**1 回だけ `linkify` を回してコミットする。** 回すまで CI が落ちる
（`linkify --check` が「直す必要がある」と言う）。

```bash
python -m tools.graph linkify
```

グラフは変わらないので、`check` の結果は前後で一致する。
差分が大きくなるが、機械的な置換であり内容は変わっていない。

## 1.10.0 — 2026-09-10

**プロジェクト自身の履歴を、上流が上書きしないようにした。**

### 何が起きていたか

`PROJECT_CHANGELOG.md` を**上流と派生の両方が自分の履歴として使っていた。**
名前が 1 つなのに所有者が 2 人いる状態で、取り込むたびに派生側の履歴が
上流の履歴で置き換わる。実際、統合型の派生 2 件は**自分の履歴を 1 行も
持たないまま、上流の履歴をそのまま抱えていた。**

### 決めたこと

**層ごとに名前を分ける。**

| ファイル | 誰の履歴か | 流れ方 |
| --- | --- | --- |
| `TEMPLATE_CHANGELOG.md` | graph-doc-template | 下流すべてに流れる |
| `PROJECT_TEMPLATE_CHANGELOG.md` | graph-project-template | その派生に流れる |
| `PROJECT_CHANGELOG.md` | **そのプロジェクト自身** | **流れない**（`merge=ours`） |

このリポジトリで足したのは `.gitattributes` の 1 行だけ。
改名は `graph-project-template` 側で行う（このリポジトリは
`PROJECT_CHANGELOG.md` を持っていない）。

### 取り込む側の作業

**通常のマージで済む。** ただし `PROJECT_CHANGELOG.md` に**上流の履歴が
入ったままのリポジトリは、中身を自分の履歴に書き換える。** 以後は上書き
されない。まだこのファイルを持っていないリポジトリは何もしなくてよい。

## 1.9.0 — 2026-09-10

**README の図が GitHub の描画上限で消える問題への対処。** 検出（`G018`）と、
回避手段（`render --aggregate`）の両方を入れた。

### 何が起きていたか

GitHub は Mermaid をエッジ 500 本までしか描かない。**500 ちょうどで既に落ちる**
（文言が `500 edges found, but the limit is 500`）。超えると図が丸ごと消え、
短いエラー文だけが残る。**壊れたことに気づけるのは GitHub 上で見たときだけ**で、
CI も `check` も何も言わなかった。

実際に 1 件で発生していた（150 ノード / 558 エッジ）。

### 足したもの

- **`G018`（新しいルール）** README に入っている図のエッジ本数を数える。
  450 以上で警告、**500 以上でエラー**。
  **グラフではなく README に実際に書き込まれた図を数える。** そうしないと
  間引いているリポジトリが誤検知で落ちる
- **`render --aggregate`** 型ごとに 1 つの箱へまとめた図を書き出す。
  エッジは「型 × 種別 × 型」に畳まれ本数が付く。
  **ノードが何件増えても図は大きくならない**
- **`Makefile` の `README_GRAPH_ARGS`** と `-include graph.mk`。
  README の図の形を**リポジトリごとに**選べる
- `graph-check.yml` の README 検査を `make readme-check` 経由に変更。
  `graph.mk` の設定を効かせるため

### 取り込む側の作業

**通常のマージで済む。** ただし `G018` は新しいエラーなので、
**既に上限に達しているリポジトリは main の CI が落ちる。** その場合だけ次を行う。

1. リポジトリのルートに `graph.mk` を作る（テンプレートは配らない。競合しない）

   ```make
   README_GRAPH_ARGS = --aggregate
   ```

2. 図を作り直してコミットする

   ```bash
   make readme
   ```

**余裕のあるリポジトリは何もしなくてよい。** 集約は個別のノードを消すので、
上限に届いていないうちに使うと、縮む余地が無いまま情報だけ失う。

いまの本数は次で分かる。

```bash
python -m tools.graph check | grep G018
```

### 上限そのものを外したいとき

`mermaid.initialize` の `maxEdges` は GitHub 側の設定で、図の中からは変えられない。
**自前でホストする HTML なら引き上げられる。** 全体図を残したいなら、README には
集約図を置き、間引かない図は GitHub Pages 側に置く。

---

## 1.8.1 — 2026-09-08

### 修正

**`/grill` の終わる条件が `check` しか挙げていなかった。**

実際に使って落とした。ADR を 1 件足して `check` だけ回し、
**README の図を作り直さずに push して CI（`render --check`）が失敗した。**

`sync` と `render --into README.md` を手順と終わる条件に足した。
**同じリポジトリを別のセッションが触っていることがある**ので、
`git add -A` ではなく自分のファイルを名指しで足すことも書いた。

---

## 1.8.0 — 2026-09-08

### 追加

**`/grill`: 要件を書き始める前に詰めるスキル**（`.claude/skills/grill/`）。

[mattpocock/skills](https://github.com/mattpocock/skills) の `grilling` を、
このテンプレートのグラフに合わせて作り直した。

- 決定木を作り、**前提が片付いた問いだけ**を 1 ラウンドにまとめて聞く
- **それぞれに推奨案と、調べた根拠を添える**
- **事実を調べるのはエージェントの仕事。** 数えられるものは数える
- frontier が空になるまで終わらない

**元のスキルとの違いは出力先。** あちらは用語集を `CONTEXT.md` に集めるが、
**こちらは作らない。** 用語はドメインノードの「用語」表にあり、`G013` が
機械で照合している。別に集めると**検査の効かない写しが 1 つ増える。**

決まったことは ADR・ユースケースの「受け入れ条件」・
ドメインノードの「定義」「不変条件」「用語」へ移す。

### 取り込む側の作業

**無い。** 通常のマージで済む。`.claude/skills/` が増えるだけで、
呼ばなければ何も起きない。

---

## 1.7.0 — 2026-09-07

### 追加

**`implemented_by`: 文書が規定している実装を指せるようにした。**

**文書と実装を同じリポジトリに置くプロジェクト向け。**
フロントマターに書くと、指し先の存在（`G016`）と、
文書と実装の片方だけが変わったこと（`G017`）を検査する。

```yaml
id: DOM-07
type: domain
title: チーム
implemented_by:
  - tournament/teams.py
```

**書かなければ何も起きない。** 用語表の「使ってはいけない言い換え」と同じ扱いで、
文書だけのリポジトリには影響が無い。

**これはエッジではない。** ソースはノードではないので、層（`G007`）にも
循環（`G006`）にも到達性（`G005`）にも関係しない。

| コード | 重要度 | 内容 |
| --- | --- | --- |
| `G016` | エラー | 指し先がリポジトリに存在しない（本文リンクの `G004` にあたる） |
| `G017` | 警告 | 文書と実装のどちらか片方だけが変わった（`G015` の実装版） |

実データで測った。**実装のコミットの 95%（tournament）と 100%（medieval-idle）が
文書の id を参照していた**（97 種・17 種、全部実在した）。
**繋がりは既にあり、確かめる手段だけが無かった。**

### 取り込む側の作業

**無い。** 通常のマージで済む。

**`implemented_by` を宣言しなければ、検査は 1 件も動かない。**

---

## 1.6.0 — 2026-09-03

### 追加

**`G015`: 依存先が変わったのに追従していない。**

依存先を書き換えたとき、それに `depends_on` / `refines` している側の一覧を出す。
規約には前から「変更の影響範囲を確かめる」と書いてあったが、
**確かめたかどうかはどこにも残らなかった。**

```bash
python -m tools.graph check                      # 未コミットの変更を見る
python -m tools.graph check --since origin/main  # 窓を広げる
```

**警告である。追従が要るとは限らない。**「見たか」を確かめるルールで、
一覧を見て「変えなくてよい」と判断したならそのまま進めてよい。

git が無い・リポジトリでない・`--no-history` を付けた場合は**何も出ない。**

**「依存先のほうが新しい」では見ていない。** その形で実データを測ると
**212 ノード中 47 件（22%）が恒常的に引っかかった。** 中心的なノードは
頻繁に触られるので、依存元は常に「古い」。腐敗ではなく定常状態である。
詳細は [graph-rules.md](docs/00-meta/graph-rules.md) の `G015` の節にある。

**CI: PR で `--since origin/<base>` を使う。** 失敗にはしない。

### 取り込む側の作業

**無い。** 通常のマージで済む。

**未コミットの変更があると `check` に `G015` が出るようになる。**
警告なので `--strict` を付けなければ失敗しない。
うるさければ `--no-history` で切れる。

---

## 1.5.2 — 2026-08-17

### 修正

**strict モードのテストが派生プロジェクトで落ちていた。**

1.5.0 のテストは、リポジトリの `docs/` を temp へコピーして
**テンプレート同梱のサンプルノードを書き換える**作りだった。
派生プロジェクトは `reset-samples` でサンプルを消しているので、
**そこでだけ失敗する。**

グラフをテストの中で組み立てるようにした。**リポジトリの `docs/` を読まない。**

### 取り込む側の作業

なし。**マージすればテストが通るようになる。**

## 1.5.1 — 2026-08-17

### 修正

**`G014` の `architecture` から「構成」を外した。**

1.5.0 で「解決したい構造上の問題」と「構成」の 2 つを求めていたが、
**構成要素を持たない方針のノードがある**（認証と権限など）。そこに
要素の表を作らせると、中身の無い節が増えるだけになる。

実プロジェクトで 1 件見つかった。既定は「解決したい構造上の問題」だけにした。

### 取り込む側の作業

なし。**警告が 1 つ減る方向の変更**なので、新しく直すものは出ない。

## 1.5.0 — 2026-08-17

### 追加

**`G014`: 種別ごとに決めた必須の節があるかを見る。**

```python
# tools/graph/schema.py
REQUIRED_SECTIONS = {
    "domain": ("定義", "不変条件", "用語"),
    "usecase": ("概要", "事前条件"),
}
```

**雛形の全節は求めない。** 「これが無いと文書として成立しない」節だけを見る。
実データで測ったところ、**全節を必須にすると 141 ノード中 50 件（35%）が落ち**、
その多くが意図的な省略だった。規約の「層を無理に埋めない」と衝突する。

- 中身までは見ない。**「なし」と書いてあれば通る**
- `refines` を持つノードには親の節を求めない（`REQUIRED_SECTIONS_REFINED`）
- **`contract` は検査しない。** 形が複数あり、共通して現れる節が無いため

**`graph review`: 本文の質を AI に見てもらう（任意）。**

```bash
python -m tools.graph review --focus DOM-01
```

**`check` からも CI からも切り離してある。** 指摘は再現せず、通信と API キーが要る。
コードは `A001`〜`A006` という別の名前空間にし、**終了コードは常に 0**。
`ANTHROPIC_API_KEY` が無ければ何もしない。詳細は `docs/00-meta/ai-review.md`。

**`contract-interaction` の雛形。** チャットの操作（コマンド・ボタン）を書くためのもの。
実プロジェクトで 10 件がこの形を雛形なしで書いていた。

**テストを新設した。** `tests/` に標準ライブラリの `unittest` で置いてある。
外部依存は増えていない。

### 変更

**CI が main への push だけ `--strict` を使うようになった。**
開発中のブランチと PR では警告を出すだけにして、書いている途中で止めない。

### 取り込む側の作業

**`G014` の警告が出たら節を足す。** 出なければ何もしなくてよい。

求めている節がプロジェクトに合わないなら、`schema.py` の `REQUIRED_SECTIONS` を
書き換える。**空のタプルにすればその種別は検査されない。**
この行がマージで競合したら**こちら側（プロジェクト側）の値を残す。**

---

## 1.4.1 — 2026-08-16

### 修正

**G013 の限界を 1 つ書き足した**（`docs/00-meta/graph-rules.md`）。
検査の挙動は変わっていない。

「兄弟どうしの用語衝突は検出されない」ことは既に書いてあったが、
**その裏返しが書かれていなかった。** 兄弟に届かない禁止語も、
**両方に依存しているノードには両方が届く。** そこでは片方の正式な用語が
もう片方の禁止語になり、正しい文章が警告される。

実プロジェクトで、形式ごとに分けた 2 つのドメインノードの一方に禁止語を足したところ、
両方を扱うユースケースが警告を受けた。直し方は既存の 2 番（用語表の側を直す）で、
**兄弟が正式に使っている語は禁止語にできない。**

### 取り込む側の作業

なし。

---

## 1.4.0 — 2026-08-16

### 追加

**`rename` がファイル名だけの変更を扱えるようになった。**

```bash
python -m tools.graph rename --from CON-04 --slug view
```

**解決している問題**: 文書の題を変えると、**ファイル名だけが実態とずれたまま残る。**
`rename` は id しか扱えず、名前を変える手段が無かった。手で直すと
相対リンクの張り替えが漏れる。

- `--to` と `--slug` はどちらか一方でよい。両方指定すれば id と名前を同時に変えられる
- `--slug` は英小文字・数字・ハイフンだけを受け付ける
- リンクの張り替えは id を変えるときと同じ経路を通る

### 取り込む側の作業

なし。

**ただし題とファイル名がずれている文書があるかもしれない。** 改題した記憶が
あるなら、`docs/` のファイル名を一度眺めるとよい。

---

## 1.3.0 — 2026-08-16

### 変更

**`sync` が目次の一覧ブロックを作り直すようになった。**
`<!-- graph:children:start -->` の中身は、同じディレクトリにあるノードを
id 順に並べたものになる。

**解決している問題**: 一覧は `new` が追記するだけで、**改題に追随しなかった。**
ノードのタイトルを変えても目次は古いままで、`check` も通ってしまう。
実プロジェクトで 3 回踏んだ。登録漏れ（`G005`）も `sync` で消えるようになる。

- ブロックの**外**に書いた案内文には触らない。手で選んだ一覧は目次に置いてよい
- 並びは id 順（`DOM-2` が `DOM-10` より前に来る）
- 目印の定義を `scaffold.py` から `schema.py` へ移した

### 修正

**`new --from` が BOM つきのファイルを読めるようになった。**
PowerShell の `Out-File -Encoding utf8` などが付ける BOM で、
1 行目の `type` が `﻿usecase` と読まれて失敗していた。

**`G013` の限界を規約に書いた**（`graph-rules.md`）。挙動は変えていない。

- 禁止語を足す前に本文を検索する。**使われている語を後から禁じると誤検知になる**
- **依存が無い者どうしの用語衝突は検出できない。** 兄弟ノードは目で見るしかない
- 照合は部分一致。禁じた語を含む新しい用語を作ると、その曖昧さも引き継ぐ

### 取り込む側の作業

**マージ後に `sync` を回すこと。** 目次の一覧が作り直され、
改題を取り残していた箇所が直る。

```bash
python -m tools.graph sync
```

一覧ブロックを手で並べ替えたり注釈を足したりしていた場合、
**その編集は失われる。** 注釈はブロックの外に移す。

---

## 1.2.3 — 2026-08-14

### 変更

**`G013` の守備範囲を規約に明記した。** 検査の動きは変わっていない。

`10-architecture` は `20-domain` に依存できないため、**アーキテクチャ文書は
ドメインの用語表で検査されない**（ADR とメタも同様）。用語としては従うが、
機械では確かめられない。この非対称が意図的なものだと分かるように書いた。

到達範囲を広げる案（依存の逆向き / `related` も辿る / グラフ全体）は
実プロジェクト 3 件で測って見送った。数字は `graph-rules.md` に残してある。

あわせて、警告への対処に 3 つ目を足した。**指しているものに名前が無いために
禁止語へ流れている**ことがあり、その場合はドメインノードに用語を足すのが正しい。

### 取り込む側の作業

なし。

**ただしアーキテクチャ文書を一度読み直すとよい。** 検査が届かない層なので、
ドメインが禁じた語がそのまま残っていることがある。派生 2 件で実際に 1 件ずつ見つかった。

---

## 1.2.2 — 2026-08-14

### 修正

**Windows で `check` が `UnicodeEncodeError` を出して落ちることがあった。**
標準出力のエンコーディングが cp932 のとき、`G013` の引用に含まれる `—`
（EM DASH）などを印字できずに例外で止まっていた。

まずいのは終了コードで、**グラフは正しいのに 1 で終わる**。警告を途中まで
出したところで死ぬので、本物の検証失敗と見分けが付かない。回避策は
`PYTHONIOENCODING=utf-8` を付けることだったが、知らなければ辿り着けない。

`main()` で標準出力・標準エラーを UTF-8 に切り替えるようにした。ファイルの
書き出しは元から UTF-8 なので、これで入出力の扱いが揃う。

- 文書の本文を引用するのは `G013` だけではないので、特定のルールではなく
  入口で直した。`stats` や `render`（標準出力）も同じ経路で助かる
- 切り替えられない標準出力（`pythonw` の `None`、テストでの差し替え）は
  そのまま素通しする。印字を安全にするための処理で落ちては本末転倒

### 取り込む側の作業

なし。通常のマージで済む。

---

## 1.2.1 — 2026-08-14

### 修正

**`upgrade` の「変更されるファイル」に `merge=ours` のものが混ざっていた。**
`README.md`・各 `index.md`・`adr-0001` は `.gitattributes` でプロジェクト側を
残すと決めてあるので、差はあってもマージでは変わらない。一覧に出るのは嘘だった。

`git check-attr merge` で見て落とすようにした。判定できないときは落とさない。

- `merge.ours.driver` が設定されていなければ `merge=ours` は効かないので、
  この場合は落とさない（README の設定手順を踏んでいないリポジトリでは本当に変わる）
- `check-attr` を実行できないときも落とさない。多めに出すのは過剰申告で済むが、
  判定できないまま落とすと「変わらない」と嘘をつくことになる

### 取り込む側の作業

なし。通常のマージで済む。

---

## 1.2.0 — 2026-08-14

### 追加

**`G013`（警告）— 依存先が禁じた言い換えの使用。** ドメインノードの「用語」表に
書いた「使ってはいけない言い換え」が、そのノードに `depends_on` / `refines` で
（間接にでも）繋がっている文書で使われていると警告する。

**解決している問題**: 規約の「`depends_on` に挙げたノードの用語だけを使って書く」は、
**唯一まだ機械で見ていない条項**だった。人の注意力だけに任されていた。

- 繋がりは間接も辿る。契約はユースケース経由でドメインに繋がるので、直接の
  `depends_on` だけでは「契約が語彙を破っている」場合を見落とす
- 「用語」表そのものは対象外。挙げていることは使っていることではない
- 末尾の丸括弧はセル全体にかかる注記として警告に添える
- 見出しと列名は `schema.py` の `TERM_SECTION_HEADING` / `TERM_COLUMN` /
  `TERM_FORBIDDEN_COLUMN` で差し替えられる。1 文字の語は対象外（`TERM_MIN_LENGTH`）

**語の意味までは分からない。** 別の概念に同じ語を使っている場合や、否定するために
持ち出した場合も引っかかる。だから警告に留めてあり、既定の CI は失敗しない。

### 変更

`git` の呼び出しに続き、本文から非散文（コード・コメント）を取り除く処理を
`rules.py` からも使うようにした（`loader.strip_non_prose`）。

### 取り込む側の作業

なし。通常のマージで済む。

**ただし「用語」表を書いてあるリポジトリでは、マージ直後に警告が増える。**
これは新しい検査が過去の記述に当たっているだけで、壊れてはいない。
`check` は警告では失敗しない。`--strict` を使っている場合だけ対応が要る。

出た警告は 2 通りに直す。**どちらが正しいかは中身を見ないと決まらない。**

1. 本文の語を正しい用語に直す
2. 用語表の側を直す（意図して使っている場面があるなら、その禁止は範囲が広すぎる）

「用語」表を書いていなければ、この検査は何もしない。

---

## 1.1.0 — 2026-08-14

### 追加

**`upgrade` コマンド。** テンプレートとの差を調べる。**読み取りのみで、
何も書き込まない。** 取り込みは従来どおり README の手順で行う。

```bash
python -m tools.graph upgrade
```

- ローカルとテンプレートの版を比べる
- 遅れていれば、その間の変更履歴をここから抜き出して表示する
- major が上がっていれば「破壊的変更を含む」と警告する
- 変更されるファイルの一覧を出す

**解決している問題**: 派生リポジトリは、マージし忘れても何も起きない。
遅れていることに気づく機会が存在しなかった。

**`tools/graph/migrations/` を新設。** 破壊的変更の移行スクリプトを置く場所。
まだ 1 つもない。書き方の約束は同ディレクトリの `__init__.py` にある。
枠組み（版の比較による自動実行）は、実例が 2〜3 個たまってから設計する。

### 変更

`git` の呼び出しを `tools/graph/git.py` に共通化した（`history.py` と `upgrade.py` で使う）。

### 取り込む側の作業

なし。通常のマージで済む。

**ただし 1.0.0 のリポジトリでは `upgrade` がまだ存在しない。**
この版に上げる最初の 1 回だけは、手動で `git fetch template && git merge template/main`
する必要がある。以後は `upgrade` で差を確認できる。

---

## 1.0.0 — 2026-08-14

版を定めた最初の版。ここまでの内容をまとめる。

### 含まれるもの

**層とノード種別**

| 層 | 種別 | 接頭辞 |
| --- | --- | --- |
| 10 | `architecture` | `ARCH-` |
| 20 | `domain` | `DOM-` |
| 30 | `usecase` | `UC-` |
| 40 | `contract` | `CON-` |
| 横断 | `adr` | `ADR-` |
| — | `index` / `meta` | `IDX-` / `META-` |

**エッジ種別** — `depends_on` / `refines` / `related` / `supersedes` / `decides`。
本文の `[[ID]]` は `mentions` として扱い、リンク切れのみ検査する。

**検証ルール** — `G001`〜`G012`。`G001`〜`G008` は構造の誤り（CI 失敗）、
`G009`〜`G012` は健全性の警告（`--strict` でのみ失敗）。

**コマンド** — `check` / `sync` / `render` / `new` / `rename` / `reset-samples` / `stats`。

**雛形** — `architecture` / `domain` / `usecase` / `usecase-runbook` /
`contract` / `contract-http` / `adr`。

**仕組み**

- 更新の取り込みは `git merge template/main`。`.gitattributes` の `merge=ours` で
  `README.md`・各 `index.md`・`adr-0001` はプロジェクト側が残る
- CI は `check` / `sync --check` / `render --into README.md --check` の 3 本
- 外部パッケージ依存なし（標準ライブラリのみ）

### 1.0.0 より前にあった破壊的変更（記録）

版を定める前に破壊的変更が 1 件あった。当時は派生プロジェクト 3 件を手で直したが、
**同種の変更が再び起きたときのために手順を残す。**

#### 層 `40-api` を `40-contracts` に改称

この層は HTTP の API に限らないため（実プロジェクトでは Discord の
インタラクションと CSV の形式が入った）、名前を実態に合わせた。
`type`・接頭辞・ディレクトリが同時に変わる。

**マージしただけでは壊れる。** 契約ノードの `type: api` が未知の語彙になり、
ノードの数だけ `G003` が出る。

```
ERROR G003 docs/40-api/api-01-....md: 未知の type 'api'（許可: ..., contract, adr）
```

移行手順:

1. `tools/graph/schema.py` の `NODE_TYPES` を更新する
   （`api` → `contract`、`API` → `CON`、`40-api` → `40-contracts`）

2. 契約ノードを 1 つずつ改名する。`type` の書き換えとファイルの移動も同時に行われる

   ```bash
   python -m tools.graph rename --from API-01 --to CON-01
   ```

3. 目次ノードは種別からディレクトリが決まらないので、移動先を明示する

   ```bash
   python -m tools.graph rename --from IDX-API --to IDX-CON --path docs/40-contracts/index.md
   ```

4. 雛形の名前を合わせる（`api.md` → `contract.md`、`api-http.md` → `contract-http.md`）

5. **散文は `rename` の対象外。** 「`40-api` ディレクトリ」のような記述は手で直す

6. `check` → `sync` → `render --into README.md` を回して確認する

`rename` は id を単語として置換し、コードブロック・コードスパン・HTML コメントの
中は書き換えない。規約に載せた使用例が壊れないようにするため。
