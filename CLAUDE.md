# CLAUDE.md

このリポジトリの設計文書は**グラフ**として管理されている。文書を書き換える前に
[docs/00-meta/graph-rules.md](docs/00-meta/graph-rules.md) を読むこと。

## 絶対に守ること

1. **文書を新規作成するときは手でファイルを置かない。** 必ず次を使う。

   ```bash
   python -m tools.graph new --type <type> --id <ID> --title "..." --slug <ascii-slug>
   ```

   手で置くと採番・配置・目次登録のどれかを必ず落とす。
   数が多いときは `--from <file>`（`type | id | title | slug` 形式）でまとめて作る。

   雛形を選ぶ場面は 2 つある。

   - HTTP の契約 → `--template contract-http`
   - **チャットの操作（コマンド・ボタン）→ `--template contract-interaction`**
   - **人間が実行する手順**（配置・更新・障害対応・移行）→ `--template usecase-runbook`

2. **文書を編集したら検証する。** 通らない状態で「完了」と報告しない。

   ```bash
   python -m tools.graph check
   ```

3. **リンクを増やしたら sync する。** ノードやエッジを増減したら README の図も再生成する。

   ```bash
   python -m tools.graph sync
   ```

   ```bash
   python -m tools.graph render --format mermaid --into README.md
   ```

   **本文に `[[ID]]` を書いたら `linkify` も回す。**

   ```bash
   python -m tools.graph linkify
   ```

   `[[ID]]` は書くときの略記で、そのままだと **GitHub 上ではただの文字として出る。**
   `linkify` が相対リンクに整形する。参照としての扱いは両方同じなので、
   グラフの検証結果は変わらない。

   3 つとも CI で最新かを検証している。忘れると落ちる。まとめて回すなら `make all`。
   **目次の一覧ブロックも `sync` が作り直す。** 改題したら回すこと。

4. **`<!-- graph:auto:start -->` 〜 `<!-- graph:auto:end -->` を手で編集しない。**
   `sync` が上書きする。ここを直したいときは、元になっているフロントマターの側を直す。

5. **依存の向きを逆にしない。** `40-contracts → 30-usecases → 20-domain → 10-architecture`。
   ドメイン文書がユースケースを参照したくなったら、それは概念の切り出しが足りない合図。
   参照を足すのではなく、概念をドメイン側に定義する。

## 文書を書くときの前提

- **前提は本文ではなくフロントマターに書く。** 本文の `[[ID]]` はリンク切れしか検査されず、
  層の逆流（`G007`）も循環（`G006`）もすり抜ける。「これがないと成立しない」ものは
  必ず `depends_on` に入れる。本文リンクは道案内に留める
- **`refines` は分割したときだけ使う。** 同じ type 同士では `depends_on` と両方書けてしまうが、
  `refines` は「もともと 1 つの文書だったものを分けた」場合に限る。
  別の概念への前提なら `depends_on`。迷ったら `depends_on` を選ぶ
- `depends_on` に挙げたノードの**用語だけ**を使って書く。挙げていない概念を持ち出さない。
  ドメインノードの「用語」表に**使ってはいけない言い換え**を書いておくと、
  依存している側で使われたときに `G013` が出る（書かなければ何も起きない）。
  **`10-architecture` は依存の向きの都合で検査されない。**
  用語としては従うが、機械では確かめられないので目で見る
- ユースケースの事後条件は、ドメインノードで定義された言葉で書く
- 契約ノードは必ず対応するユースケースを `depends_on` に持つ。持てないなら
  ユースケースが未定義なので、先にそちらを書く
- **契約かどうかは「片方だけが古いまま動きうるか」で判定する。**
  常に一緒に変わるものは契約ではない（方針なら `10-architecture`、それ以外は実装の詳細）。
  「外部かどうか」で判断しない
- 決定を変えるときは既存文書を書き換えず、新しい ADR を起こして `supersedes` で繋ぐ
- **題を変えたらファイル名も合わせる。** `rename --from <ID> --slug <新しい名前>` で、
  id を据え置いたままファイル名と全参照を張り替えられる
- **層を無理に埋めない。** 題材によっては書くことが本当に無い層がある。
  上位層の言い換えで埋めると重複が増えるだけ。薄いままでよい
- **手順の順序はグラフに乗らない。** `depends_on` は前提であって「この後にやる」ではない。
  順序は本文に書く（`usecase-runbook` 雛形の「順序の制約」）

## 文脈の取り方

全文書を読み込まない。対象ノードとその近傍だけを読む。

```bash
python -m tools.graph render --format json --focus UC-01 --depth 1
```

これが「前提（`depends_on` 先）」と「影響範囲（そのノードを指している側）」の
両方を含んだ最小の集合になる。返ってきた `nodes` のファイルだけを読む。

範囲が足りなければ `--depth 2` に広げる。全体を見たいときだけ `--focus` を外す。

```bash
python -m tools.graph render --format json --out docs/graph.json
```

## 変更の影響範囲を確かめる

ノードを変更したら、そのノードを `depends_on` している側が追従不要かを確認する。
**`check` がその一覧を出す**（`G015`）。**未コミットの変更を見ているので、
コミットする前に回す。**

```bash
python -m tools.graph check
```

**追従が要るとは限らない。**「見たか」を確かめるルールで、「直せ」ではない。
見て「変えなくてよい」と判断したなら、そのまま進めてよい。

コミット済みの変更まで見るときは窓を指定する。

```bash
python -m tools.graph check --since origin/main
```

各文書末尾の「このノードを参照しているノード」も同じ一覧である
（`sync` が生成しているので手で数えない）。図で見るなら次を使う。

```bash
python -m tools.graph render --format mermaid --focus DOM-01 --depth 1
```

## よくある落とし方

| 症状 | 原因 | 対処 |
| --- | --- | --- |
| `G005` が出る | 目次から辿れない | `sync` を回す。一覧ブロックは `sync` が作り直す |
| `G004` が出る | `id` の綴り間違い、または参照先が未作成 | `stats` で存在する id を確認 |
| `G007` が出る | 依存の向きが逆 | 概念を下位層に移す。向きだけ変えて意味が壊れないか確認する |
| `G001` が出る | フロントマターにネストしたマップを書いた | サブセット記法のみ。`graph-rules.md` の例に合わせる |
| `G011` が出る | `draft` のまま放置されている | 確定させるか削除する。意図的に寝かせているなら理由を本文に書く |
| `G012` が出る | 1 ノードに参照が集まりすぎ | 概念が混ざっていないか点検する。中心的な語彙なら放置してよい |
| `G013` が出る | 依存先が禁じた言い換えを使っている | 正しい用語に直す。意図した使い方なら、禁止の範囲が広すぎるので用語表を直す |
| `G016` が出る | `implemented_by` の指し先が無い | パスを直すか宣言を外す。**同じリポジトリの中しか指せない** |
| `G017` が出る | 文書と実装の片方だけを書き換えた | もう片方を見て追従が要るか確かめる。**要らなければそのままでよい** |
| `G015` が出る | 依存先を書き換えたが、依存元を見ていない | 挙がったノードを開いて追従が要るか確かめる。**要らなければそのままでよい** |
| `G014` が出る | 種別に必要な節が無い | 節を足す。**書くことが無いなら「なし」と書く。** 求めるほうが誤りなら `schema.py` の `REQUIRED_SECTIONS` を直す |

## テンプレート由来のファイルを直すとき

このリポジトリが `graph-doc-template` から起こしたものなら、`tools/` と
`docs/00-meta/` はテンプレート側と共通で、更新は `git merge template/main` で入る。

- **共通ファイルを手でコピーして同期しない。** 削除が伝わらず、ドリフトする
- 共通ファイルに手を入れるなら、テンプレート側に入れてからマージで戻す。
  こちらだけ直すと次のマージで競合する
- 取り込み手順は README の「テンプレートの更新を取り込む」にある。
  **`reset-samples --yes` まで含めて 1 セット**

## テンプレート本体を変えるとき

**このリポジトリがテンプレート本体（`graph-doc-template`）なら、
変更を `TEMPLATE_CHANGELOG.md` に記録し、`tools/graph/version.py` の版を上げる。**

| 変更 | 上げる箇所 |
| --- | --- |
| 取り込む側に作業が要る（語彙の変更、id 規約の変更） | major |
| 追加だけ（雛形、コマンド、ルール） | minor |
| 文言・不具合の修正 | patch |

**破壊的な変更には必ず移行手順を書く。** 書けないなら入れない。
手順のない破壊的変更は、長く放置されたリポジトリを黙って壊す。

## ツールを直すとき

- 語彙（ノード種別・エッジ種別・層）は `tools/graph/schema.py` に集約されている。
  ここを変えたら `docs/00-meta/node-types.md` の表も必ず合わせる
- 検証ルールは `tools/graph/rules.py` に 1 ルール 1 関数で並んでいる。
  ルールを足したら `RULE_INDEX` と `graph-rules.md` の一覧の両方に追記する
- 外部依存を足さない。標準ライブラリだけで動くことがこのツールの前提。
  **`check` はオフラインで完結させる。** 通信するのは `review` だけで、
  あれは CI から呼ばず、終了コードも常に 0（`docs/00-meta/ai-review.md`）
- **ルールを足したらテストも足す。** `tests/` に `unittest` で置く。
  `python -m unittest discover -s tests -t .` が CI の 1 本目
- **テキストを書き出すときは必ず `encoding="utf-8"` と `newline="\n"` を渡す。**
  既定に任せると Windows だけ CRLF・cp932 になり、**git が正規化するので
  `git status` にも出ない。** `test_newline_discipline` が構文木で見ている
- **動く版の下限は `version.py` の `MIN_PYTHON`。** 開発機・CI・本番で実際に
  回している版であって、願望ではない。動かすなら CI の `python-version` と
  README も同時に直す（`test_python_floor` がずれを落とす）

## 文書と実装が同じリポジトリにあるとき

このリポジトリは `graph-project-template` から起こしたもので、
**設計文書と実装が同居している。**

### 開発の流れは META-05 にある

**順番・関門・ブランチ・コミット・リリースは
[META-05](docs/00-meta/dev-flow.md) が決めている。**
この文書はプロジェクトごとに書き換えてよい既定値なので、**毎回そちらを読む。**
ここに書いてあることと食い違っていたら、**META-05 のほうが正しい。**

**とくに `/grill` を回す場面が 4 つ決まっている。**

| いつ | 合図 |
| --- | --- |
| issue を書く前 | 「決めること」が 3 つ以上ある、または埋められない |
| ADR を起こす前 | **却下した案が書けない** |
| 実装に着手する前 | ユースケースの「受け入れ条件」が空、または検証できない書き方 |
| `schema.py` の語彙・層を変える前 | ノード種別・エッジ種別・層に手を入れようとしている |

**該当したら、書き始める前に回す。** 回さずに書くと、ADR が
「決めた記録」ではなく「揺れた記録」になる。

### implemented_by を書く

文書が規定している実装を、ノードのフロントマターで指す。

```yaml
implemented_by:
  - app/tournament/teams.py
```

- **リポジトリの根からのパスで書く。** 先頭は実装を置いたディレクトリの名前である。
  **`app/` でも `src/` でもよい**（[META-04](docs/00-meta/implementation-layout.md)）。
  **テンプレートは名前を決めていない。**上の `app/` は例にすぎない
- **指し先が消えたら `G016` がエラーになる。** ファイル名を変えたら一緒に直す
- **片方だけ変えると `G017` が出る。** どちらが遅れているかは機械には分からない
- **全ノードに書かない。** ドメインと契約には対応がある。
  アーキテクチャと ADR には無いことが多い。**無いなら書かない**

### 要件は issue から始める

| 段階 | どこに書くか |
| --- | --- |
| 何が困っているか、何を決めるか | **issue**（`.github/ISSUE_TEMPLATE/requirement.md`） |
| なぜその判断にしたか、却下した案 | **ADR** |
| 振る舞いと受け入れ条件 | **ユースケースの「受け入れ条件」** |
| 概念と言葉 | **ドメインノードの「定義」「用語」** |
| 実装 | ソース（`implemented_by` で文書から指す） |

**issue に書いたまま終わらせない。** issue は流れ、文書は残る。
決まったことを文書へ移すまでが 1 つの要件である。

**issue を実装計画の置き場にしない。** 受け入れ条件はユースケースが持っており、
決定は ADR が持っている。**issue に同じものを書くと、2 箇所を保つことになる。**

### PR にはノードの id を書く

CI（`app-check.yml`）が**実在するかを確かめる。**
対応する文書が無い変更（依存の更新など）は `なし` と書く。

### CI は 2 本ある

| ファイル | 見るもの | 誰のものか |
| --- | --- | --- |
| `graph-check.yml` | 文書のグラフ | **テンプレートが配る。書き換えない** |
| `app-check.yml` | 実装のテストと PR の参照 | **プロジェクトのもの**（`merge=ours`） |

実装のテストを `graph-check.yml` に書き足さないこと。
テンプレートを取り込むたびに競合する。

## 要件が曖昧なとき

**書き始める前に詰める。** `.claude/skills/grill/` にスキルがある。

```
/grill
```

決定木を作り、**前提が片付いた問いだけ**を推奨案つきでまとめて聞き、
決まったことを ADR・ユースケースの受け入れ条件・ドメインノードの用語表へ移す。

**用語集（`CONTEXT.md`）は作らない。** 用語はドメインノードの「用語」表にあり、
`G013` が機械で照合している。別に集めると検査の効かない写しが増える。

## 実装を書くときの決まり

### モジュールの説明に、規定しているノードの id を書く

**1 行目に書く。** これが `implemented_by` の根拠になる。

```python
"""チームと所属。UC-09〜UC-15。"""
```

**推測で対応を決めない。** 実プロジェクトでは、この 1 行があったおかげで
`implemented_by` を推測せずに起こせた（tournament のコミットの 95%、
medieval-idle の 100% がノード id を参照していた）。

同じ理由で、**コミットと PR にもノードの id を書く。**
PR は CI（`app-check.yml`）が実在を確かめる。

### 識別子はドメインの正式な用語を使う

**`G013` はコードに届かない。** 用語表の照合は文書の本文だけを見ている。
実装の識別子が用語表とずれても、機械は何も言わない。

だから**書くときに合わせる。** 「エントリー」を `application` と名付けると、
そこから先の会話が二重になる。**ドメインノードの「用語」表が正しい。**

利用者に返す文言は例外で、**`G013` が届く**（契約ノードの応答表に
写しているため）。実際にそこで実装の欠陥が見つかったことがある。

### コメントには「なぜやらなかったか」を書く

コードをなぞるコメントは書かない。**残す価値があるのは、
読んだだけでは分からないこと**である。

- なぜ別の手を採らなかったか
- どのノードのどの判断に従っているか（id を書く）

**「なぜそうしたか」が長くなるなら、それは ADR に書くべきもの。**
コメントに設計判断を書くと、文書と実装の 2 箇所に決定が散る。
