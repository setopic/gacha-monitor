# graph-project-template の変更履歴

**この層だけの履歴。** 履歴は 3 つに分かれており、**それぞれ持ち主が違う。**

| ファイル | 誰の履歴か | 派生への流れ方 |
| --- | --- | --- |
| [TEMPLATE_CHANGELOG.md](TEMPLATE_CHANGELOG.md) | `graph-doc-template`（検証ツール） | 下流すべてに流れる |
| **このファイル** | `graph-project-template`（統合型の層） | その派生に流れる |
| [PROJECT_CHANGELOG.md](PROJECT_CHANGELOG.md) | **そのプロジェクト自身** | **流れない**（`merge=ours`） |

**派生プロジェクトはこのファイルを書き換えない。** 自分の履歴は
`PROJECT_CHANGELOG.md` に書く。

現在の版: **0.8.2**

---

## 0.8.2 — 2026-09-11

### 上流

`graph-doc-template` 1.14.2 を取り込んだ（本文の AI レビューの指摘。`META-01` の
`G015`〜`G019` を 5 節へ戻し、雛形の一覧に `contract-interaction` を足し、
サンプルノード 4 件を直したもの）。

**取り込みで `graph-rules.md` が競合した。** 上流が 5 節へ移した `G015`〜`G019` が、
こちら側では旧位置に残っていたため。移動後を採り、旧位置の重複を落としている。

### 直したもの

同じレビューで、**この層だけが持つ文書**にも 2 件の指摘が出た。

**`META-05` の「リリースと配置」の表に `PROJECT_TEMPLATE_CHANGELOG.md` が無かった。**
履歴は 3 つに分かれているのに表は 2 つしか載せておらず、**この層の履歴の行き先が
どこにも書かれていなかった。** 実際、1.14.1 を取り込んで 0.8.1 を書いたときも、
行き先は `META-05` からは分からず git の履歴から読んでいる。

**`META-04` の節「要件は issue から始める」が、末尾で自分を否定していた。**
題は「issue から始める」という順序を宣言しているのに、末尾は
「ここが決めているのは『どこに書くか』だけである」と書いていた。
**入口を決めているのは事実なので、末尾のほうを実態に合わせた。**
`CLAUDE.md` の同じ節にはこの一文が無いので、そちらは触っていない。

### 派生プロジェクトに伝えること

**`META-05`（`dev-flow.md`）は `merge=ours` なので、この直しは流れない。**
各プロジェクトの `dev-flow.md` は自分のものである。履歴の行き先で迷ったことが
あるなら、同じ行を自分で足しておくとよい。

**サンプルノードを撤去しているリポジトリでは、上流の取り込みで競合が出る。**
`DOM-01` などが modify/delete として挙がるので、削除側を採る。

---

## 0.8.1 — 2026-09-11

### 上流

`graph-doc-template` 1.14.1 を取り込んだ（雛形の本文をレビューし、
`作成日: {{DATE}}` を 6 つの雛形から外した。派生 7 リポジトリの 198 ノードで
残っていたのは 11 件だけで、運用として成立していなかった）。

あわせて `adr` の日付と状態に記入案内が足され、`contract-interaction` の
「無し」が「なし」に統一され、`usecase` の「概要」表に記入案内が足された。

**この層で足すものは無い。**

### 派生プロジェクトに伝えること

**取り込んでも既存の文書は変わらない。** マージのあと `new` で起こした
ノードに `作成日:` が付かなくなるだけで、既にある `作成日:` を消す必要も無い。

雛形を自分のプロジェクト向けに書き換えている場合だけ競合することがある。
**そのときは自分の側を残してよい。**

---

## 0.8.0 — 2026-09-11

### 上流

`graph-doc-template` 1.14.0 を取り込んだ（`G019`: 表が途中で切れていないかを見る）。

**この層で足すものは無い。**

### 直したもの

**README のルール一覧が `G014` で止まっていた。**`G015`・`G016`・`G017`・`G018`・
`G019` が丸ごと欠けており、**1.6.0（`G015`）から遅れ続けていた。**

`README.md` は `.gitattributes` で `merge=ours` なので、**上流でルールを足しても
伝播しない。** 共有ファイルの `docs/00-meta/graph-rules.md` は正しく伝播しており、
ずれるのは `merge=ours` のものだけである。README がその代表になる。

**同じ取り込みで、README のコマンド表が切れていたのも直した。**1.8.0 で
スキルの案内文を表の行と行の間に挿し込んでおり、**以降の 14 行が本文として
流れていた。** これが `G019` を入れるきっかけになった 2 件のうちの 1 件である。

### 派生プロジェクトに伝えること

**表が切れている文書があれば、取り込んだ時点で main の CI が落ちる。**
落ちるのは正しく、その表は取り込む前から GitHub 上で壊れて表示されている。

```bash
python -m tools.graph check | grep G019
```

**あわせて README のずれも見ておくとよい。** `merge=ours` のファイルは
黙って遅れるので、気づく機会が取り込みのときしかない。

```bash
git diff HEAD template/main -- README.md
```

---

## 0.7.0 — 2026-09-10

### 追加

**開発の流れを [META-05](docs/00-meta/dev-flow.md) として書き下した。**

これまで定義されていたのは**成果物の整合性**（`check`・`G0xx`・`implemented_by`）
だけで、**いつ何をするか**はどこにも書かれていなかった。ブランチの切り方、
コミットの粒度、PR にする条件、レビューの有無、リリースの扱いが全部そうだった。

**`merge=ours` で守っている。** ブランチの切り方もリリースの仕方も
プロジェクトごとに違う。テンプレートが配るのは**既定値**であって規約ではないので、
書き換えたものが取り込みで戻らないようにした。

**`/grill` を回す場面を 4 つに決めた。** これが今回の主眼である。
スキルは 1.8.0 から同梱されていたが、**いつ使うかがどこにも書かれていなかった。**

| いつ | 合図 |
| --- | --- |
| issue を書く前 | 「決めること」が 3 つ以上ある、または埋められない |
| ADR を起こす前 | **却下した案が書けない** |
| 実装に着手する前 | 受け入れ条件が空、または検証できない書き方 |
| `schema.py` の語彙・層を変える前 | 全ノードに波及し、着手してからでは戻せない |

**強制はしない。** grill を回したかは CI から見えないので、
表には「回さないと何が起きるか」まで書いてある。**代償を先に見せる形にした。**

**`CONTRIBUTING.md` を足した。** GitHub が issue / PR の作成画面に自動で
リンクするための入口で、**META-05 を指すだけで中身は持たない。**
`merge=ours` にしていないのは、指し先のパスが全プロジェクトで同じで、
プロジェクト側が書き換える理由が無いため。

### 実態から書いた

**PR もタグも全リポジトリで 0 件だった。** ブランチは `main` だけ。
つまり PR テンプレートと `app-check.yml` の `refs` ジョブは**一度も動いていない。**

そこで「全部 PR にする」とは書かず、**PR のときだけ動く検査 2 つ**を挙げて、
それが要る変更だけを PR にすると決めた。

| 検査 | 直接 push だと |
| --- | --- |
| `check --since origin/<base>` | `G015` の窓が広がらない。**コミットすると見えなくなる** |
| `app-check.yml` の `refs` | 走らない |

**PR を全部に強制すると、この 2 つが「いつもの儀式」になって読まれなくなる。**

### 上流

`graph-doc-template` 1.13.0 を取り込んだ（`/grill` の差分表・調査の非ブロッキング規則・
起動する場面の追加）。

### 取り込む側の作業

なし。通常のマージで済む。

**ただし META-05 は既定値なので、自分のやり方と違うなら書き換える。**
書き換えても取り込みで戻らない。変えたことは `PROJECT_CHANGELOG.md` に書く。

---

## 0.6.0 — 2026-09-10

### 上流

`graph-doc-template` 1.12.0 を取り込んだ（サンプルの改名と README の整合）。

**この層で足すものは無い。**

**派生プロジェクトに伝えること。** サンプルと同じファイル名を自分のノードに
使っている場合は、**取り込む前に改名する。** そのままだと修正/削除の競合になり、
サンプルの中身が自分の文書に混ざる。

```bash
python -m tools.graph rename --from ARCH-01 --slug <このプロジェクトらしい名前>
```

## 0.5.0 — 2026-09-10

### 上流

`graph-doc-template` 1.11.0 を取り込んだ（`linkify`）。

**この層で足すものは無い。**

**派生プロジェクトに伝えること。** 1 回だけ `python -m tools.graph linkify` を
回してコミットする。回すまで CI が落ちる。グラフは変わらないので `check` の
結果は前後で一致するが、**差分は大きくなる**（本文の `[[ID]]` が全部
相対リンクに変わるため）。

## 0.4.0 — 2026-09-10

**履歴のファイル名を層ごとに分けた。**

### 何が起きていたか

このファイルはもともと `PROJECT_CHANGELOG.md` という名前だった。
ところが**派生プロジェクトも同じ名前で自分の履歴を書こうとする。**
名前が 1 つで所有者が 2 人いる状態で、取り込むたびに派生側が上書きされる。

実際、統合型の派生 2 件（`tournament-docs` / `medieval-idle-docs`）は
**自分の履歴を 1 行も持たないまま、この層の履歴をそのまま抱えていた。**

### 決めたこと

- この層の履歴を `PROJECT_TEMPLATE_CHANGELOG.md` に改名した
- `PROJECT_CHANGELOG.md` は**プロジェクト自身のもの**とし、雛形だけを配る
- 上流（1.10.0）の `.gitattributes` が `PROJECT_CHANGELOG.md merge=ours` を持つ

### 取り込む側の作業

**`PROJECT_CHANGELOG.md` に、この層の履歴（「graph-project-template の変更履歴」
で始まるもの）が入っているなら、中身を自分の履歴に書き換える。**
以後は上書きされない。この層の履歴は `PROJECT_TEMPLATE_CHANGELOG.md` で読む。

---

## 0.3.0 — 2026-09-10

### 上流

`graph-doc-template` 1.9.0 を取り込んだ（`G018` と `render --aggregate`）。

**この層で足すものは無い。** 統合型に固有の事情は無く、文書だけの型と同じように効く。

**派生プロジェクトに伝えること。** `G018` は新しいエラーなので、README の図が
既に GitHub の描画上限（エッジ 500 本）に達しているリポジトリは、取り込んだ時点で
main の CI が落ちる。落ちるのは正しく、その README の図は**取り込む前から
GitHub 上で描画されていない**。直し方は `TEMPLATE_CHANGELOG.md` の 1.9.0 にある。

## 0.2.0 — 2026-09-08

### 追加

**`CLAUDE.md` に「実装を書くときの決まり」を足した。**

- **モジュールの説明の 1 行目に、規定しているノードの id を書く。**
  これが `implemented_by` の根拠になる。実プロジェクトではこの 1 行のおかげで
  対応を推測せずに起こせた（tournament 95% / medieval-idle 100%）
- **識別子はドメインの正式な用語を使う。**`G013` はコードに届かないので、
  機械では確かめられない。書くときに合わせる
- **コメントには「なぜやらなかったか」を書く。**
  長くなるならそれは ADR に書くべきもの

記事（[keitakn/engineering-skills](https://github.com/keitakn/engineering-skills)）の
`code-naming` / `code-comments` に相当するものだが、**汎用のガイドは入れていない。**
このプロジェクトには用語表という具体的な正解があり、汎用の命名論より強い。

### 上流

`graph-doc-template` 1.8.0 を取り込んだ（`/grill` スキル）。

---

## 0.1.1 — 2026-09-07

### 直したもの

- **`CLAUDE.md` の `implemented_by` の例が根からのパスだと読めなかった。**
  `- tournament/teams.py` とだけ書いてあり、**どこからのパスかを言っていなかった。**
  実装を `app/` の下に置いた派生（tournament / medieval-idle のどちらもそうしている）で
  そのまま真似すると、指し先が無いので **`G016` がエラーになる。**
  例を `app/tournament/teams.py` に変え、**根からのパスであること**と、
  **先頭のディレクトリ名はテンプレートが決めていないこと**（[META-04](docs/00-meta/implementation-layout.md)）を書き足した。
  **`app/` を規約にしたわけではない。**0.1.0 の「足さなかったもの」のとおり、
  実装のディレクトリ名は自由なままである

### 変えていないもの

- **`docs/00-meta/graph-rules.md` の同じ例。**あちらは上流（`graph-doc-template`）と
  共通のファイルで、**文書だけの型には `app/` が無い。**
  こちらで直すと次の `git merge template/main` で競合する

---

## 0.1.0 — 2026-09-07

最初の版。`graph-doc-template` 1.7.0 から起こした。

### 足したもの

- **[META-04](docs/00-meta/implementation-layout.md)** — 文書と実装を同じリポジトリに
  置く理由、置き方、対応の粒度、既にある実装の合流手順
- **`.github/workflows/app-check.yml`** — 実装の検査と、PR が指すノードが実在するかの確認。
  `graph-check.yml` とは別ファイルにして `merge=ours` で守っている。
  **実装のテストを `graph-check.yml` に足すと、テンプレートを取り込むたびに競合する**
- **`.github/scripts/check_refs.py`** — PR 本文のノード id を解決する。
  `なし` と書けば通す
- **`.github/ISSUE_TEMPLATE/requirement.md`** と **`pull_request_template.md`**
- **`.gitignore`** — `.env` と実行時にできるものを除外
- **`CLAUDE.md`** に「文書と実装が同じリポジトリにあるとき」の節

### 足さなかったもの

- **実装計画のファイル。** 受け入れ条件はユースケースが、決定は ADR が持っている。
  3 つ目の文書種別を作ると、同じことを 2 箇所に書くことになる
- **実装のディレクトリ名の規約。** `src/` でも `tournament/` でもよい。
  予約しているのは `docs/` と `tools/graph/` だけ
- **`tools/` への変更。** `implemented_by` と `G016` / `G017` は上流
  （`graph-doc-template` 1.7.0）に入れてある。こちらで書き換えると、
  以後の取り込みで `rules.py` と `schema.py` が競合する
