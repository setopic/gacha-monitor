# tools/graph は常に LF で書き出す（sync が改行を書き換える）。
# 作業コピーも LF に固定しないと、sync のたびに全行差分になる。
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary

# テンプレートから更新を取り込む（git merge template/main）とき、
# ここに挙げたものは常にプロジェクト側を残す。中身がプロジェクト固有だから。
# 取り込む側で一度だけ設定が要る:
#   git config merge.ours.driver true
#
# index.md はスラッシュを含まないので、どの階層の index.md にも当たる。
# 目次の一覧はプロジェクトのノードで埋まっているため、常に自分側が正しい。
README.md merge=ours
index.md  merge=ours
docs/50-adr/adr-0001-graph-driven-docs.md merge=ours

# PROJECT_CHANGELOG.md は**そのリポジトリ自身の履歴**であって、上流のものではない。
# 上流が同じ名前で自分の履歴を持っていると、取り込みのたびに上書きするか
# 全面的に競合する。**上流の履歴は別の名前で流す。**
#
#   TEMPLATE_CHANGELOG.md          graph-doc-template（このリポジトリ）の履歴
#   PROJECT_TEMPLATE_CHANGELOG.md  graph-project-template の履歴
#   PROJECT_CHANGELOG.md           そのプロジェクト自身の履歴。上流は触らない
PROJECT_CHANGELOG.md merge=ours
