# gacha-monitor の変更履歴

**このプロジェクトの履歴。** 上流の履歴は別のファイルにあり、
取り込み（`git merge template/main`）で流れてくる。

| ファイル | 誰の履歴か |
| --- | --- |
| [TEMPLATE_CHANGELOG.md](TEMPLATE_CHANGELOG.md) | `graph-doc-template`（検証ツール） |
| [PROJECT_TEMPLATE_CHANGELOG.md](PROJECT_TEMPLATE_CHANGELOG.md) | `graph-project-template`（統合型の層） |
| **このファイル** | **このプロジェクト自身。** 上流は触らない |

現在の版: **0.1.4**

---

## 0.1.4 — 2026-09-20

**19 桁の投稿 ID がスプレッドシートで数値に変換され、二重通知の抑止が効いていなかった。**
`CON-03` `UC-01` と実装。

通知が 3 日届かないという報告から調べた。**止まっていた原因はこれではない。**
閾値を超えた投稿が 1 件も無かっただけで、`UC-01` A1 のとおりの正常な沈黙だった。
これはその過程で見つけた別の不具合である。

| 何が問題だったか | どう直したか |
| --- | --- |
| 投稿 ID を書式指定なしで置いていた。**スプレッドシートがこれを数値に変換する**ため、倍精度で表せる 16 桁を超えた下位の桁が落ちる。読み戻した ID は元と一致せず、`readNotifiedStages_` の照合が常に外れる。**同じ投稿の同じ段階が毎日通知されていた**（`UC-01` の事後条件に反する） | 追記する範囲の投稿 ID 列を**テキスト書式にしてから書く**（`appendRows_`）。新規作成時は列全体に当てる（`setIdColumnText_`）。**既存シートには列の書式が無いので、追記のたびに当て直す** |
| 既にある行の ID は壊れたまま残る。落ちた桁はその列からは戻せない | `repairPostIds()` で URL の列から復元する。**URL は文字列なので無傷で残っており、末尾が投稿 ID そのものである。** 配置後に 1 回だけ実行する |

**0.1.2 と同じ型の不具合である。** あちらは `2026-09` が日付値に、こちらは 19 桁の
ID が数値に変換された。`CON-03` の注意書きを日付だけでなく**長い数字に見える値**まで
広げ、両方を表に並べた。

**`CON-01` と `CON-02` は変えていない**（`G017` は出るが、見たうえで追従不要と判断した）。

### 分かったこと

**この不具合は通知を増やす向きに働いていた。** 直すと通知はさらに減る。
沈黙の原因とは逆を向いているので、閾値の話とは切り離して扱う。

**閾値（`ADR-0008`）は変えない。** 追跡日数 2 では 1 投稿あたりの観測が 2 回になり、
見える最大値は `ADR-0007` の実測でいう「1 日後」の水準（平均 546）に下がる。
閾値 1,000 に対して鳴りにくいことは承知のうえで、現状の刻みを維持する。

---

## 0.1.3 — 2026-09-15

**追跡期間を 4 日から 2 日に、閾値を 500/1,000/2,000 から 1,000/2,000/4,000 にした。**
`ADR-0007` `ADR-0008`（`ADR-0003` `ADR-0004` を置き換え）。

1 週間の運用で、**同じ投稿を複数回測った推移**が初めて手に入った。
ADR-0003 の決定は投稿ごとに 1 点しか無い断面から出していたので、
経過日数ごとの中央値を別々の投稿で比べるしかなかった。今回は同一の投稿を追えている。

| 何が分かったか | 数字 |
| --- | --- |
| 伸びは 1 日でほぼ終わる | 平均保存数は 当日 313 → 1 日後 546（**+75%**）→ 2 日後 609（+11%）→ 3 日後 568（**−7%**） |
| 4 回目の取得は減少側に働く | 追跡中の 37 件のうち **23 件が減り、12 件が増え、2 件が横ばい**。増えた 12 件も幅は +1〜+7 |
| ADR-0003 の見直し基準は「該当なしに等しい」 | 形式上 6 件該当したが、増加幅 +1〜+7 に対し次の段階まで 97〜277 の開き |
| 500 の段は多すぎた | 新しい刻みでの通知量は 1,000 で 2.9 件/日、2,000 で 1.9 件/日、4,000 で 0.3 件/日 の計 **約 5.1 件/日** |

**保存数は減ることがある。** 伸び切ったあと保存を外す人がいるためで、
`DOM-02` のライフサイクルに書き足した。

### 変えたもの

| 何を | 変更 |
| --- | --- |
| 設定シート（運用中の値） | 追跡日数 4 → 2、閾値 500/1,000/2,000 → 1,000/2,000/4,000 |
| `app/monitor.gs` の初期値と既定値 | 同上。**新規導入時に生きている決定と食い違わないようにする** |
| `ARCH-01` の未決事項 | 「4 日より延ばすべきか」は決着したので削除 |
| `DOM-02` のライフサイクル・用語 | 同一投稿を追った実測に差し替え。閾値の参照先を ADR-0008 へ |
| `ARCH-02` `CON-01` の参照先 | ADR-0003 → ADR-0007 |

**判定ロジックは変えていない。** 追跡日数も閾値も設定シートの値で、
コードが変わったのは初期値と既定値だけである。

### 費用

**月約 2,500 円 → 約 1,200 円。** 追跡の窓に入る投稿が 1 回あたり
約 109 件から約 52 件に減る。

---

## 0.1.2 — 2026-09-14

**月の利用額が毎回 0 に戻る不具合を直した。UC-02 / UC-03 / CON-03 と実装。**

運用 5 日目に、週次サマリーが出す額（$0.47）と X Developer Portal の実額（$3.33）が
合わないことに気づいた。**実額のほうが正しく、設計どおりだった。**
5 日ぶんの取得 563 件と、先行して回した検証の 101 件を足すと $3.34 になり、
Portal の表示とほぼ一致する。壊れていたのは集計の側だけだった。

| 何が問題だったか | どう直したか |
| --- | --- |
| 設定シートの「対象月」に `2026-09` を書式指定なしで置いていた。**スプレッドシートがこれを日付値に変換する**ため、読み戻すと `Date` になり、文字列としての比較が毎回外れる。月が変わったと判定されて取得件数が実行のたびに 0 に戻り、その日ぶんだけが残っていた | 日付に見える値は**テキスト書式を指定してから書く**（`setConfigCell_`）。読む側も `Date` と文字列の両方を同じ鍵に正規化する（`asMonthKey_` / `asDayKey_`）。**書く側だけを直すと、既存シートに残った日付値で再発する** |
| 同じ UTC 日に 2 回動くと、2 回目も件数に足していた。X は同じ投稿を 24 時間 UTC 以内に 1 回しか課金しないので、実額より多く見える | 「課金済み UTC 日」を設定シートに持ち、同じ日の 2 回目以降は加算しない（`UC-03` の代替フロー A3） |
| 初回だけ発生するユーザー情報の取得を、投稿と同じ単価で数えていた（単価が違う） | 概算から外した。**概算であることを通知の文言にも出す**（`UC-02` 手順 4） |

**`CON-01` と `CON-02` は変えていない。** 単価も通知の本文も規定していないため
（`G017` は出るが、見たうえで追従不要と判断した）。

### 分かったこと

**実勢の月額は約 2,500 円**（追跡窓に入る投稿が 1 実行あたり平均 113 件）。
当初の見積り 1,640 円は投稿頻度を 18.3 件/日として出したもので、
運用 5 日の実測では 21〜33 件/日と幅があった。予算 3,000 円には収まっている。

---

## 0.1.1 — 2026-09-11

**設計文書 15 ノードを AI レビューにかけ、指摘 14 件を直した。実装は変えていない。**

### 層をまたぐもの（1 コミットでまとめて直した）

依存元がすべて同じ 9 ノードに収まるので、一度に直せば `G015` は出ない（実際 0 件）。

| 何が問題だったか | どう直したか |
| --- | --- |
| 課金の単位が 4 箇所に「24 時間 UTC 以内」「同じ日」の 2 通りで書かれ、どちらの意味か決まらなかった | `CON-01` を単一の正にした。**UTC 基準であること**と、**日次の起動が 9 時台（JST）＝ UTC 00:00〜01:00 で課金の区切りのすぐ内側にある**ことを明記 |
| 「上限」が 1 ページの上限（100 件）と打ち切りの上限の 2 つの意味で使われ、後者の数がどこにも無かった | **3 ページ（300 件）で打ち切る**と書き、2 つを区別した |
| 通知ログへの書き込み順が、契約（送信後）とユースケース（異常は送信前）で逆 | 種別 `異常` だけ順序が逆であることと理由を、契約とユースケースの両方に明記 |
| `ADR-0005` の影響が `CON-01` に除外の指定を含めよと書いているのに、入力の表に無かった | 除外の指定（`replies,retweets`、**引用は除けない**）を追加 |
| `UC-02` の事前条件「記録がある」が、自分の代替フロー A1 と矛盾 | 事前条件から外した。**沈黙と故障を見分けるのが目的**なので、記録の有無で止めてはいけない |
| `DOM-02` の必須列が `DOM-01` と別の意味になり、「6 種類すべてを記録する」と矛盾 | 6 指標すべて ○ に揃え、「判定に使うか」とは別の話だと書いた |
| `DOM-01` の属性表に、毎日変わる `経過日数` が必須で並んでいた | 導出値であることを明記 |
| `CON-01` の失敗表に接続エラー / 5xx が無く、`UC-03` の検知対象と食い違っていた | 行を追加 |
| `CON-02` の失敗が `UC-01` E4 だけを指していたが、呼ぶ側 3 つで扱いが違う | 呼ぶ側ごとの表を追加 |

### 順次直したもの

**`ADR-0003` の見直しの条件が、実行できない状態だった。** 「閾値の 8 割」とだけ
書いてあるが、同じ日の `ADR-0004` で閾値は 3 段階になっている。8 割が
400 / 800 / 1,600 のどれかで結果が変わる。**「まだ届いていない次の段階の 8 割」**と
定義し直し、段階ごとの下限を表にした。**決定（追跡期間 4 日）は変えていない。**

**この集計は運用フォローアップとして予定されている。** 日次スナップショットが
2〜3 週間貯まった時点で回す。

`ADR-0004` の却下案で「1.4 件/週」と「週 2 件」が食い違っていたのも直した。

### 実装との関係

`G017` が契約 3 件に出たが、**追従は不要**と判断した。書いたのは実装が既に
行っていることで、`app/monitor.gs` と突き合わせて確認している。

| 文書の記述 | 実装 |
| --- | --- |
| 異常は記録 → 送信、どちらも握りつぶして元の例外を投げ直す | 155〜164 行 |
| 通知・週次は送信 → 記録 | 226 行 / 447 行 |
| 除外は `replies,retweets`、ページ上限 3 | 259 行 / `maxPages: 3` |

---

## 0.1.0 — 2026-09-10

最初の版。**実装と運用が先にあり、設計文書を後から起こした。**

### 実装

- `app/monitor.gs` 日次の取得・判定・記録・通知。2026-09-10 から実運用中
- `app/spike.gs` 検証用。取得の可否と指標の水準を 1 回だけ測る

### 設計文書 15 ノード

要件を詰める過程（8 ラウンド）で決まったことを、根拠となった実測値ごと残した。
**半年後に「なぜこうしたか」を再現できることを優先している。**

| ADR | 決めたこと |
| --- | --- |
| [ADR-0002](docs/50-adr/adr-0002-save-count-as-metric.md) | 主指標に保存数を採用する（表示数を見送った理由と実測値） |
| [ADR-0003](docs/50-adr/adr-0003-tracking-window.md) | 追跡期間を 4 日にする（費用の構造と、観測できない限界） |
| [ADR-0004](docs/50-adr/adr-0004-three-thresholds.md) | 閾値を 3 段階にする |
| [ADR-0005](docs/50-adr/adr-0005-filter-in-code.md) | 転載と返信をコード側で除外する |
| [ADR-0006](docs/50-adr/adr-0006-apps-script-runtime.md) | 実行基盤に Apps Script を選ぶ |

### CI 3 本

| ファイル | 見るもの |
| --- | --- |
| `graph-check.yml` | 文書のグラフ（テンプレートが配る。書き換えない） |
| `app-check.yml` | Apps Script の構文、マニフェスト、**監視対象がコードに残っていないか** |
| `deploy.yml` | main への push で Apps Script へ配置する |
| `docs-html.yml` | 設計文書を HTML にして GitHub Pages で公開する |

### 公開にあたって

**このリポジトリは公開されている。** 監視対象はコードにも文書にも書かず、
設定シートの「監視アカウント」にだけ置く。設定シートの初期値は空にしてあり、
`app-check.yml` が書き戻されていないかを毎回確かめる。

実測値（保存数の分布、投稿頻度、費用）は**数字だけ**を文書に残した。
どのアカウントを見て得た数字かは書いていない。

### 未確定の論点

**追跡 4 日で足りているかは分かっていない。** 4 日で打ち切る以上、
5 日目以降の値は原理的に観測できない。判断の材料と手順は
[ADR-0003](docs/50-adr/adr-0003-tracking-window.md) の「見直しの条件」にある。
