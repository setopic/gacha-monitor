## 何をしたか

<!-- 1〜3 行。詳細はコミットと文書にある -->

## 対応するノード

<!-- id を書く（DOM-07 / CON-08 / ADR-0065）。
     CI が「実在するか」を確かめる。**書き忘れると落ちる。**
     文書に対応が無い変更（設定・依存の更新など）は `なし` と書く -->

## 確認したこと

- [ ] `python -m tools.graph check --since origin/main` を回した
- [ ] `G015` / `G017` に挙がったノードを開き、追従が要るかを判断した
- [ ] 追従が要らないと判断したものは、**その理由を自分の言葉で言える**

<!-- ノードを増やした / 決定を変えた / schema.py の語彙を触ったなら、
     着手前に /grill を回しているはず。回していないなら、
     この PR は「決めた記録」ではなく「揺れた記録」を残そうとしている（META-05） -->

- [ ] ADR を含むなら、**却下した案が書いてある**
